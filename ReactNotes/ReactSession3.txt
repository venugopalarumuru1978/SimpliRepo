Today's Agenda
---------------
Props
Routing
Forms
---------------
npm create vite@latest my-react-app
npm run dev
---------------
Props

“props” is a special React keyword for proprietary purposes, used for data transmission from component to component. 

props are read-only.

Steps :- 

Create two components like parent and child. here data has to be transfered from parent to child. 

1. Create a parent component, within that componnent call the child component with arguments as follows. 
	<ChildComponentName variable=value variable=value .... />
2. Create a child component, while creating use "props" object as a  formal argument for a component function/method

function ChildComp(props)
{

}
3. Now get values from props object as follows in child component
	props.variable  => will get value
--------------------------
Routing :- 

Navigating b/w components

Steps:- 

1.  Install following package to work with routing?
		npm install react-router-dom
			you will get following classes
				Link, BrowserRouter, Routes, Route
2.  Create a functional component for NavBar then define all the links as follows
		"Link" class is meant for providing hyper link
import { Link } from "react-router-dom";

export default function Navbar()
{
    return(
        <div>
            <ul>
                <Link to="/"><li>Home</li></Link>
                <Link to="/login"><li>Login</li></Link>
                <Link to="/register"><li>Register</li></Link>
                <Link to="/contact"><li>Contact</li></Link> 
            </ul>
        </div>
    );
}
3.  Goto App.js File then define navigators as follows

	import { BrowserRouter, Route } from 'react-router-dom';
	import { Routes } from 'react-router';

	function App() {
  return (
    <div className="App">
      <BrowserRouter>
      <Navbar />
      <Routes>
        <Route path="/" exact Component={Home} />
        <Route path="/login" exact Component={Login} />
        <Route path="/register" exact Component={Register} />
        <Route path="/contact" exact Component={Contact} />
      </Routes>
      </BrowserRouter>
    </div>
  );
--------------------------
useNavigate() :-  this function/hook is used to navigate with components dynamically
Step 1:- declare object within the function
const object = useNavigate()
Step 2 :- use object  with route url defined in <Route> tag for the component
	object('/url')
--------------------------
How to add bootstrap to the react application

1. Install following package
	npm install bootstrap // install latest version
		or
	npm install bootstrap@5.3.8 // specific version

2. Add following import statement in index.js/main.jsx file
	import 'bootstrap/dist/css/bootstrap.min.css'; 
----------------------------------
...(Spread Operator) :-  it is used to concatenate two or more than two object values into single unit.

let APcities = ["Amaravathi", "Vizag", "Nellore"]
let TSCities = ["Hyderabad", "Warangal", "KarimNagar"]

let cities = [...APCities, ...TSCities];
--------------------------